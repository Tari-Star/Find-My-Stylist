<div class="row">
{{!-- <div class="column">
    {{#each stylists as |stylist|}}
    <div class="card">
        {{> stylist-info stylist}}
    </div>
      {{/each}}
</div> --}}
<div class="column">
    {{#each posts as |post|}}
    <div class="card">
        {{> post-info post}}
    </div>
      {{/each}}
</div> 
</div>